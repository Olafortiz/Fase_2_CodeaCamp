<div class="container">
  <h2>Introduce tu url</h2>
  
  <%= form_for :url, :url => { :action => :create } do |f| %>

  <div class="field">
    <%= f.label :long_url %>
    <%= f.text_field :long_url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
  <% end %>

  </div>